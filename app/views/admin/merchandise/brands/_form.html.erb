<% if @brand.errors.any? %>
  <div id="error_explanation">
    <h2 class="page-title"><%= pluralize(@brand.errors.count, "error") %> prohibited this brand from being saved:</h2>

    <ul>
    <% @brand.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<label>Name</label>
<%= f.text_field :name %>
<%if current_user%>
	<%=f.hidden_field :user_id ,:value=> current_user.id%>
<%end%>